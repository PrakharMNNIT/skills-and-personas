[project]
name = "prax-teach-v2-runtime"
version = "0.1.0"
description = "Pinned runtime dependencies for the Prax Teach v2 learner-state and scheduling tools"
requires-python = ">=3.10,<3.14"
dependencies = [
  "fsrs==6.3.1",
]

[tool.uv]
package = false


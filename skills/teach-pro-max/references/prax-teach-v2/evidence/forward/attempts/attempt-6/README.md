# Attempt 6 — failed and stopped

Attempt 6 began as a fresh all-case rerun after the attempt-5 composite was
declared release-ineligible. Five cases were generated in separate
`fork_turns:none` child tasks. The accessibility interaction case failed its
hard gate because the response did not state that host-UI accessibility remains
unverified and did not explicitly name host-chat delivery without custom
artifact controls.

The run was stopped at the first discovered hard-gate failure. It was never
represented as a completed eight-case run and received no independent passing
receipt. The five exact normalized outputs are retained in `outputs/`; the
candidate instruction was then tightened before the next full all-eight rerun.

Claim boundary: this is failed public agent-behavior evidence, not learner or
accessibility-field evidence.

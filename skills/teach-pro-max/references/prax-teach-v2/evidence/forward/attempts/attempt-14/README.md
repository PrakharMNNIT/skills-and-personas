# Forward attempt 14

Attempt 14 passed the independent rubric scorer at 8/8 with 20 exact-byte
bindings and a byte-exact execution replay. It was then rejected by the
independent frozen-spec reviewer because the practical transcript moved to the
unseen transfer case before explicitly eliciting the learner's explanation.
The receipt's sequence finding therefore overstated the bound evidence.

The exact rubric, run, receipt, outputs, context, execution fixture, and seven
source snapshots are retained here. This attempt is historical rejected
evidence only and is ineligible for the current release gate.

# Resumed scaffolded evidence

No filesystem workspace was read; I’m resuming only from your supplied snapshot.

Saved learner-authored response: `Use the index for WHERE a = 42 because a is the leading indexed column.` It was correct, but `hint_level: 3 / strongest_hint` makes this scaffolded application evidence—not unassisted mastery.

**Your action:** Given an index on `(customer_id, created_at)`, which query matches it better, and why?

A. `WHERE created_at >= '2026-08-01'`  
B. `WHERE customer_id = 17 AND created_at >= '2026-08-01'`

If unsure, say “I don’t know.”

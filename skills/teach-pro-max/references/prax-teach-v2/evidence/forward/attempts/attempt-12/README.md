# Forward attempt 12

Attempt 12 was rejected before a receipt was issued. The independent scorer
recomputed all 20 rubric, run, source, context, output, and execution bindings
and replayed the executable fixture byte-for-byte. The lesson case nevertheless
failed because its closing retrieval said only “later” and “next retrieval”
without an explicit time horizon.

The scorer also found two routing defects in the exact bound source: the course
prompt routed as an ordinary engineering task, and the practical teaching prompt
routed as quick instead of lesson. The rubric, run, context, execution fixture,
outputs, and seven bound source snapshots are retained here. They are historical
failed evidence only; no passing receipt exists for this attempt.

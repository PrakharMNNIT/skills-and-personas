# Idempotence

Idempotence means performing an operation multiple times has the same effect as performing it once. Example: setting a thermostat to 20°C twice leaves it at 20°C, just as setting it once does.

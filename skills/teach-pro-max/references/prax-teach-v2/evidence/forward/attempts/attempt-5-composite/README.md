# Attempt 5 — independently passed, release-ineligible composite

Attempt 5 independently passed the then-current public rubric 8/8 and matched
all 16 declared hashes. The exact run manifest, independent receipt, context,
rubric, and eight learner-facing outputs are retained here.

It reused six passing attempt-4 outputs and reran only the two attempt-4
failures without an intervening instruction change. A later release audit
correctly rejected that as insufficient evidence for the specified
fix-and-rerun loop. The package sources and accessibility rubric were then
changed. Attempt 5 is historical engineering evidence only and is ineligible
for the current release gate.

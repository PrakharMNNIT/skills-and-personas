# Forward-behavior attempt 16

Attempt 16 passed its independent score, then became historical when
`scripts/praxteach/routing.py` changed to bind the runtime contracts tree.

This archive preserves the exact rubric, run, receipt, outputs, context,
execution fixture, and seven source files bound by the run. The archived
routing source was mechanically reconstructed from its single subsequent
change and matches the run's recorded SHA-256 exactly.

# Forward-behavior attempt 17

Attempt 17 passed its independent 8/8 score with all 20 bindings and three
byte-exact executable replays. It became historical when independent review
required direct authoritative-model preference and corrected canonical film
route commands in two bound source documents.

This archive preserves the exact rubric, run, receipt, outputs, context,
execution fixture, and seven source files bound by that run.

# Truthful engineering handoff

The local candidate contains a separately versioned, deterministic, offline Visual Lab with shared Web Components, Python floating-point, Rubik's, and lost-update lessons, a build-time-only formal receipt boundary, static fallback, and local receipts. Full engineering verification passed with 212 Python tests, 19 root Node tests, 8 runtime Node tests, package validation, exact Markdown/HTML parity, registry checks, Ruff lint/format, trusted macOS sandbox E2E, and no-network runtime checks.

The nested Codex desktop sandbox rejects a child `sandbox-exec`, so the first in-sandbox attempt was recorded as environment-limited. The final trusted run was completed outside that nested boundary with escalation and passed. The four external learner gates remain `waiting_external`: representative accessibility, immediate outcomes, delayed 7–14 day retention, and novel transfer/generalization. No scientific learning claim is made.

Promotion remains gated: no global skill replacement, merge, push, deploy, publication, participant recruitment, paid model call, or deletion was performed.

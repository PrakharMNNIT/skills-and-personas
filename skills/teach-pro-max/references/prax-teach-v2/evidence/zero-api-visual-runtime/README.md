# Zero-API Visual Runtime evidence

This directory contains generated baseline, build, test, review, and packaging receipts for the local candidate. Evidence must be recomputed on the exact candidate bytes. The four external gates remain `waiting_external` until genuine observations exist.
